Current Issues:
    10/05/2021: After adding package structure, main.py stopped working when
    run through Windows CMD (by double-clicking on the file from Windows GUI). 
    Program still successfully runs from a text-editor, including debug mode.